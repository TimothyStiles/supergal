{
  "name": "supergal",
  "version": "0.3.0",
  "description": "Get yourself an open source license.",
  "main": "index.js",
  "scripts": {
    "build": "npm install package.json && npm install -g",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Timothy Stiles",
  "license": "Apache-2.0",
  "bin": {
    "supergal": "./index.js"
  },
  "dependencies": {
    "commander": "^2.15.0"
  }
}
